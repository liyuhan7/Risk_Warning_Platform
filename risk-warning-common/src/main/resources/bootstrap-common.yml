spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        group: DEFAULT_GROUP
        shared-configs:
          - data-id: common-dev.yaml
      discovery:
        enabled: true
        server-addr: 127.0.0.1:8848
